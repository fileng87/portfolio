import GuestbookLayout from '@/components/Gusetbook/GuestbookLayout';
import React from 'react';

type Props = {};

const GuestBook = (props: Props) => {
  return <GuestbookLayout />;
};

export default GuestBook;
